# 账号与公开许可预检

核对日期：2026-09-26。

## 结论

所有者已在本任务中明确表示 GitHub 与 Cloudflare 均已登录，并授权 Web/PWA 的 staging 与 production 部署、公开发布以及随站点内容的分发。该授权登记为本次操作层面的所有者确认；它不等于独立法律意见或第三方书面许可证明。

本次只读检查发现：目标 GitHub 仓库在 Package82 开始前已经是 public，GitHub Pages 已返回 HTTP 200；这不是 Package82 新建或发布的结果。远端 `main` 当时为 `e6ec63e3f13b13e6dda46b8ff4e6d43ebcdd55a8`，远端工作流仍含 `push` 到 `main` 后自动发布。Package82 本地工作流已改成仅 `workflow_dispatch`。

## 账号预检

- GitHub：浏览器会话由所有者确认已登录；本机没有可调用的 GitHub CLI，因此未做 CLI 写操作。
- Cloudflare：浏览器会话由所有者确认已登录；`wrangler 4.141.0 whoami` 显示本机 CLI 未认证，因此未部署 Worker、未创建 Turnstile widget、未设置或读取 secret。
- DeepSeek：未读取账号、余额、账单、API key 或自动充值状态；真实调用 0 次，实际费用 US$0。

## 内容与许可登记

| 内容                                  | 本任务登记                         | 仍需保留的证据                                                          |
| ------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------- |
| HK-ELE 数据、principal 排名与词族资料 | 所有者明确授权 Web/PWA 公开分发    | 项目内部权属或授权文件；不得把 Stage 4 sensitivity 当 principal         |
| HK-ELE 名称、图标、颜色及品牌         | 所有者明确授权本次公开使用         | 品牌权属/授权记录                                                       |
| CPB 100                               | 所有者明确确认可随站点分发         | 仓库中仍没有独立第三方书面许可文书；如机构流程要求，push 前补存书面证据 |
| 软件依赖 notices                      | 现有 `THIRD_PARTY_NOTICES.md` 保留 | 发布制品的 MIT/MPL notices 复核                                         |
| Native OCR/ML Kit/Apple Vision        | 不在本次公开范围                   | Native AI 与原生二进制继续延期                                          |

目标仓库当前没有 `LICENSE` 文件。公开可访问不自动授予他人复制、修改或再分发权。所有者仍需决定是否加入代码许可证，以及数据、品牌和 CPB 是否采用单独条款；在决定前可维持“保留所有权利”，但应明确写入仓库。

## 操作门槛

所有者授权已经记录；不过手动操作者在首次 push 前仍应确认两点：一是 CPB 书面依据是否满足所属机构要求，二是是否需要添加代码/数据许可文本。Package82 没有替所有者编造许可证。
